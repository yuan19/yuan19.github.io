<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Android系统启动</title>
    <url>/2020/07/13/Android%E7%B3%BB%E7%BB%9F%E5%90%AF%E5%8A%A8/</url>
    <content><![CDATA[<h2 id="启动流程"><a href="#启动流程" class="headerlink" title="启动流程"></a>启动流程</h2><p>顺序进程： <strong>init进程—-&gt;Zygote进程—-&gt;SystemServer进程—-&gt;App进程</strong></p>
<p><img src="/images/android-boot.jpg" alt="image"></p>
<a id="more"></a>

<h2 id="init进程"><a href="#init进程" class="headerlink" title="init进程"></a>init进程</h2><p>如上图，OS开机后，会先去预加载BootLoader，启动Linux内核，再启动init进程。</p>
<p>BootLoader：主要将操作系统OS拉起来运行的小程序。</p>
<p>￼init进程代码文件： system/core/init/init.cpp</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>** argv)</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>init进程主要是解析init.rc文件启动Zygote进程。</p>
<h2 id="Zygote进程"><a href="#Zygote进程" class="headerlink" title="Zygote进程"></a>Zygote进程</h2><p>启动Zygote会调用进程main入口，在main方法中，再去调用ZygoteInit，ZygoteInit是属于Java FrameWork， 这样就从C++调用至Java。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// framework/base/cmds/app_process/app_main.cpp</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>* <span class="keyword">const</span> argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">while</span> (i &lt; argc) &#123;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span>* arg = argv[i++];</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">strcmp</span>(arg, <span class="string">"--zygote"</span>) == <span class="number">0</span>) &#123;</span><br><span class="line">            zygote = <span class="literal">true</span>;</span><br><span class="line">            niceName = ZYGOTE_NICE_NAME;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">strcmp</span>(arg, <span class="string">"--start-system-server"</span>) == <span class="number">0</span>) &#123;</span><br><span class="line">            startSystemServer = <span class="literal">true</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">strcmp</span>(arg, <span class="string">"--application"</span>) == <span class="number">0</span>) &#123;</span><br><span class="line">            application = <span class="literal">true</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">strncmp</span>(arg, <span class="string">"--nice-name="</span>, <span class="number">12</span>) == <span class="number">0</span>) &#123;</span><br><span class="line">            niceName.setTo(arg + <span class="number">12</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">strncmp</span>(arg, <span class="string">"--"</span>, <span class="number">2</span>) != <span class="number">0</span>) &#123;</span><br><span class="line">            className.setTo(arg);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            --i;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">if</span> (zygote) &#123;</span><br><span class="line">        runtime.start(<span class="string">"com.android.internal.os.ZygoteInit"</span>, args, zygote);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (className) &#123;</span><br><span class="line">        runtime.start(<span class="string">"com.android.internal.os.RuntimeInit"</span>, args, zygote);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"Error: no class name or --zygote supplied.\n"</span>);</span><br><span class="line">        app_usage();</span><br><span class="line">        LOG_ALWAYS_FATAL(<span class="string">"app_process: no class name or --zygote supplied."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在ZygoteInit类的main入口方法中，会去创建一个ZygoteServer对象，ZygoteServer的registerServerSocketFromEnv方法会去创建Socket的Server端，它用来等待ActivityManagerService请求Zygote来创建新的应用进程。forkSystemServer()方法去创建SystemServer进程，而runSelectLoop是一直循环监听Socket是否AMS有创建新进程。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// framewrok/base/core/java/com/android/internal/os/ZygoteInit.java</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String argv[])</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 创建ZygoteServer对象</span></span><br><span class="line">    ZygoteServer zygoteServer = <span class="keyword">new</span> ZygoteServer();</span><br><span class="line">    <span class="keyword">try</span>&#123;</span><br><span class="line">        zygoteServer.registerServerSocketFromEnv(socketName);</span><br><span class="line">        ...</span><br><span class="line">        <span class="keyword">if</span> (startSystemServer) &#123;</span><br><span class="line">          	<span class="comment">// 创建SystemServer进程</span></span><br><span class="line">            Runnable r = forkSystemServer(abiList, socketName, zygoteServer);</span><br><span class="line">        &#125;</span><br><span class="line">        ...</span><br><span class="line">        caller = zygoteServer.runSelectLoop(abiList);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>ZygoteServer.java文件中方法registerServerSocketFromEnv，runSelectLoop。runSelectLoop中connection.processOneCommand会去生成新建进程</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// framewrok/base/core/java/com/android/internal/os/ZygoteServer.java</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">registerServerSocketFromEnv</span><span class="params">(String socketName)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (mServerSocket == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">int</span> fileDesc;</span><br><span class="line">        <span class="keyword">final</span> String fullSocketName = ANDROID_SOCKET_PREFIX + socketName;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            String env = System.getenv(fullSocketName);</span><br><span class="line">            fileDesc = Integer.parseInt(env);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (RuntimeException ex) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(fullSocketName + <span class="string">" unset or invalid"</span>, ex);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            FileDescriptor fd = <span class="keyword">new</span> FileDescriptor();</span><br><span class="line">            fd.setInt$(fileDesc);</span><br><span class="line">            mServerSocket = <span class="keyword">new</span> LocalServerSocket(fd);</span><br><span class="line">            mCloseSocketFd = <span class="keyword">true</span>;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException ex) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(</span><br><span class="line">                    <span class="string">"Error binding to local socket '"</span> + fileDesc + <span class="string">"'"</span>, ex);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">Runnable <span class="title">runSelectLoop</span><span class="params">(String abiList)</span> </span>&#123;</span><br><span class="line">	...</span><br><span class="line">	<span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">		...</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = pollFds.length - <span class="number">1</span>; i &gt;= <span class="number">0</span>; --i) &#123;</span><br><span class="line">			...</span><br><span class="line">			<span class="keyword">if</span> (i == <span class="number">0</span>) &#123;</span><br><span class="line">				ZygoteConnection newPeer = acceptCommandPeer(abiList);</span><br><span class="line">				peers.add(newPeer);</span><br><span class="line">				fds.add(newPeer.getFileDesciptor());</span><br><span class="line">			&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">try</span> &#123;</span><br><span class="line">					ZygoteConnection connection = peers.get(i);</span><br><span class="line">					<span class="keyword">final</span> Runnable command = connection.processOneCommand(<span class="keyword">this</span>);</span><br><span class="line">					...</span><br><span class="line">				&#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">					...</span><br><span class="line">				&#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">					...</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>总结：</p>
<ul>
<li><p>Zygote进程如何被启动</p>
</li>
<li><p>Zygote创建SystemServer，通过Socket等待创建新APP进程</p>
</li>
<li><p>如何从C++ Framework至Java Framework</p>
</li>
</ul>
<h2 id="SystemServer进程"><a href="#SystemServer进程" class="headerlink" title="SystemServer进程"></a>SystemServer进程</h2><p>由Zygote的方法forkSystemServer去创建SystemServer进程，创建进程后进入方法main，然后再去启动各种服务。比如： ActivityManagerService，PowerManagerService， LocationManagerService等</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// framewrok/base/service/java/com/android/server/SystemServer.java</span></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * The main entry point from zygote.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">new</span> SystemServer().run();</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// Start services.</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            traceBeginAndSlog(<span class="string">"StartServices"</span>);</span><br><span class="line">            startBootstrapServices();</span><br><span class="line">            startCoreServices();</span><br><span class="line">            startOtherServices();</span><br><span class="line">            SystemServerInitThreadPool.shutdown();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">            Slog.e(<span class="string">"System"</span>, <span class="string">"******************************************"</span>);</span><br><span class="line">            Slog.e(<span class="string">"System"</span>, <span class="string">"************ Failure starting system services"</span>, ex);</span><br><span class="line">            <span class="keyword">throw</span> ex;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            traceEnd();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>总结：</p>
<ol>
<li>重新再看启动流程： init –&gt; Zygote –&gt; SystemServer</li>
<li>Zygote核心：负责所有进程的孵化，创建Socket，无限循环等待AMS调用创建进程</li>
<li>SystemServer核心：初始化启动各种服务</li>
</ol>
<p>参考</p>
<p><a href="http://gityuan.com/android/" target="_blank" rel="noopener">Android 操作系统架构开篇</a></p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
  </entry>
  <entry>
    <title>LocationManager解析(基于10.0)</title>
    <url>/2020/07/11/LocationManager%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<h2 id="1-应用"><a href="#1-应用" class="headerlink" title="1. 应用"></a>1. 应用</h2><p>LocationManager应用例子，locationManager.getBestProvider获取最好的定位方式，locationManager.requestLocationUpdates是开始获取定位。需要获取定位，有设备GPS方式，另外一种是NLP方式，就是NETWORK_PROVIDER，NETWORK_PROVIDER由第三方提供，由系统集成NLP模块获取定位。<a id="more"></a></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Criteria criteria = <span class="keyword">new</span> Criteria();</span><br><span class="line">criteria.setAccuracy(Criteria.ACCURACY_COARSE);<span class="comment">//低精度，如果设置为高精度，依然获取不了location。</span></span><br><span class="line">criteria.setAltitudeRequired(<span class="keyword">false</span>);<span class="comment">//不要求海拔</span></span><br><span class="line">criteria.setBearingRequired(<span class="keyword">false</span>);<span class="comment">//不要求方位</span></span><br><span class="line">criteria.setCostAllowed(<span class="keyword">true</span>);<span class="comment">//允许有花费</span></span><br><span class="line">criteria.setPowerRequirement(Criteria.POWER_LOW);<span class="comment">//低功耗</span></span><br><span class="line"><span class="comment">//获取LocationManager</span></span><br><span class="line">LocationManager locationManager = (LocationManager) getSystemService(Context.LOCATION_SERVICE);</span><br><span class="line"><span class="comment">// 获取最好的定位方式</span></span><br><span class="line">String provider = locationManager.getBestProvider(criteria, <span class="keyword">true</span>); <span class="comment">// true 代表从打开的设备中查找</span></span><br><span class="line"><span class="comment">// 获取所有可用的位置提供器</span></span><br><span class="line">List&lt;String&gt; providerList = locationManager.getProviders(<span class="keyword">true</span>);</span><br><span class="line"><span class="comment">// 测试一般都在室内，这里颠倒了书上的判断顺序</span></span><br><span class="line"><span class="keyword">if</span> (providerList.contains(LocationManager.NETWORK_PROVIDER)) &#123;</span><br><span class="line">    provider = LocationManager.NETWORK_PROVIDER;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (providerList.contains(LocationManager.GPS_PROVIDER)) &#123;</span><br><span class="line">    provider = LocationManager.GPS_PROVIDER;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// 当没有可用的位置提供器时，弹出Toast提示用户</span></span><br><span class="line">    Toast.makeText(<span class="keyword">this</span>, <span class="string">"Please Open Your GPS or Location Service"</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line">Log.d(<span class="string">"provider"</span>, <span class="string">" provider = "</span> + provider);</span><br><span class="line">LocationListener locationListener = <span class="keyword">new</span> LocationListener() &#123;</span><br><span class="line">    <span class="comment">//当位置改变的时候调用</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onLocationChanged</span><span class="params">(Location location)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//经度</span></span><br><span class="line">        <span class="keyword">double</span> longitude = location.getLongitude();</span><br><span class="line">        <span class="comment">//纬度</span></span><br><span class="line">        <span class="keyword">double</span> latitude = location.getLatitude();</span><br><span class="line"></span><br><span class="line">        <span class="comment">//海拔</span></span><br><span class="line">        <span class="keyword">double</span> altitude = location.getAltitude();</span><br><span class="line"></span><br><span class="line">        locationStr = longitude + <span class="string">"_"</span> + latitude;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//当GPS状态发生改变的时候调用</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onStatusChanged</span><span class="params">(String provider, <span class="keyword">int</span> status, Bundle extras)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">switch</span> (status) &#123;</span><br><span class="line">            <span class="keyword">case</span> LocationProvider.AVAILABLE:</span><br><span class="line">                message = <span class="string">"当前GPS为可用状态!"</span>;</span><br><span class="line">                Log.d(<span class="string">"provider"</span>, <span class="string">" message = "</span> + message);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> LocationProvider.OUT_OF_SERVICE:</span><br><span class="line">                message = <span class="string">"当前GPS不在服务内!"</span>;</span><br><span class="line">                Log.d(<span class="string">"provider"</span>, <span class="string">" message = "</span> + message);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> LocationProvider.TEMPORARILY_UNAVAILABLE:</span><br><span class="line">                message = <span class="string">"当前GPS为暂停服务状态!"</span>;</span><br><span class="line">                Log.d(<span class="string">"provider"</span>, <span class="string">" message = "</span> + message);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//GPS开启的时候调用</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onProviderEnabled</span><span class="params">(String provider)</span> </span>&#123;</span><br><span class="line">        message = <span class="string">"GPS开启了!"</span>;</span><br><span class="line">        Log.d(<span class="string">"provider"</span>, <span class="string">" message = "</span> + message);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//GPS关闭的时候调用</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onProviderDisabled</span><span class="params">(String provider)</span> </span>&#123;</span><br><span class="line">        message = <span class="string">"GPS关闭了!"</span>;</span><br><span class="line">        Log.d(<span class="string">"provider"</span>, <span class="string">" message = "</span> + message);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//获取上次的location</span></span><br><span class="line">Location location = locationManager.getLastKnownLocation(provider);</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 参1:选择定位的方式</span></span><br><span class="line"><span class="comment"> * 参2:定位的间隔时间</span></span><br><span class="line"><span class="comment"> * 参3:当位置改变多少时进行重新定位</span></span><br><span class="line"><span class="comment"> * 参4:位置的回调监听</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">locationManager.requestLocationUpdates(provider, <span class="number">10000</span>, <span class="number">0</span>, locationListener);</span><br></pre></td></tr></table></figure>

<h2 id="2-启动"><a href="#2-启动" class="headerlink" title="2. 启动"></a>2. 启动</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">systemRunning</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ...    </span><br><span class="line">	<span class="comment">// prepare providers：系统一启动，就开始准备Provider</span></span><br><span class="line">	initializeProvidersLocked();</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">initializeProvidersLocked</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// 生成GPS_PROVIDER对象</span></span><br><span class="line">	LocationProvider gnssProviderManager = <span class="keyword">new</span> LocationProvider(GPS_PROVIDER, <span class="keyword">true</span>);</span><br><span class="line">    <span class="comment">// mRealProviders数据保存该值</span></span><br><span class="line">    mRealProviders.add(gnssProviderManager);</span><br><span class="line">    <span class="comment">// 添加至mProviders列表中</span></span><br><span class="line">    addProviderLocked(gnssProviderManager);</span><br><span class="line">	GnssLocationProvider gnssProvider = <span class="keyword">new</span> GnssLocationProvider(mContext,</span><br><span class="line">		gnssProviderManager,</span><br><span class="line">		mHandler.getLooper());</span><br><span class="line">	gnssProviderManager.attachLocked(gnssProvider);  </span><br><span class="line">    ...</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// bind to network provider：NETWORK_PROVIDER默认，是为空，需要第三方修改再提供APK，然后系统再修改，指定相应的包名，功能Feature为NLP</span></span><br><span class="line">    LocationProvider networkProviderManager = <span class="keyword">new</span> LocationProvider(NETWORK_PROVIDER, <span class="keyword">true</span>);</span><br><span class="line">    LocationProviderProxy networkProvider = LocationProviderProxy.createAndBind(</span><br><span class="line">            mContext,</span><br><span class="line">            networkProviderManager,</span><br><span class="line">            NETWORK_LOCATION_SERVICE_ACTION,</span><br><span class="line">            com.android.internal.R.bool.config_enableNetworkLocationOverlay,</span><br><span class="line">            com.android.internal.R.string.config_networkLocationProviderPackageName,</span><br><span class="line">            com.android.internal.R.array.config_locationProviderPackageNames);</span><br><span class="line">    <span class="keyword">if</span> (networkProvider != <span class="keyword">null</span>) &#123;</span><br><span class="line">        mRealProviders.add(networkProviderManager);</span><br><span class="line">        addProviderLocked(networkProviderManager);</span><br><span class="line">        networkProviderManager.attachLocked(networkProvider);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        Slog.w(TAG, <span class="string">"no network location provider found"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    ...</span><br><span class="line">	<span class="comment">// bind to geocoder provider：经纬度解析为位置文本 Provider</span></span><br><span class="line">    mGeocodeProvider = GeocoderProxy.createAndBind(mContext,</span><br><span class="line">		com.android.internal.R.bool.config_enableGeocoderOverlay,</span><br><span class="line">		com.android.internal.R.string.config_geocoderProviderPackageName,</span><br><span class="line">		com.android.internal.R.array.config_locationProviderPackageNames);</span><br><span class="line">    ...  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-使用流程"><a href="#3-使用流程" class="headerlink" title="3. 使用流程"></a>3. 使用流程</h2><p>基于上面应用的例子，LocationManager.getProviders(true);通过LocationManager，用Binder再调用至LocationManagerService，查找LocationManagerService中的方法，重点流程方法： getBestProvider， getProviders， requestLocationUpdates</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">getBestProvider</span><span class="params">(Criteria criteria, <span class="keyword">boolean</span> enabledOnly)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//criteria：获取要求</span></span><br><span class="line">    <span class="comment">//enabledOnly:获取当前Provider中，可用或者不可用。Provider可以添加，但未启用</span></span><br><span class="line">    List&lt;String&gt; providers = getProviders(criteria, enabledOnly);</span><br><span class="line">    <span class="keyword">if</span> (providers.isEmpty()) &#123;</span><br><span class="line">        providers = getProviders(<span class="keyword">null</span>, enabledOnly);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!providers.isEmpty()) &#123;</span><br><span class="line">        <span class="keyword">if</span> (providers.contains(GPS_PROVIDER)) &#123;</span><br><span class="line">            <span class="keyword">return</span> GPS_PROVIDER;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (providers.contains(NETWORK_PROVIDER)) &#123;</span><br><span class="line">            <span class="keyword">return</span> NETWORK_PROVIDER;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> providers.get(<span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> List&lt;String&gt; <span class="title">getProviders</span><span class="params">(Criteria criteria, <span class="keyword">boolean</span> enabledOnly)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 判断权限</span></span><br><span class="line">	<span class="keyword">int</span> allowedResolutionLevel = getCallerAllowedResolutionLevel();</span><br><span class="line">	<span class="keyword">synchronized</span> (mLock) &#123;</span><br><span class="line">        <span class="comment">// mProviders在 “2启动” addProviderLocked(gnssProviderManager);方法中</span></span><br><span class="line">		ArrayList&lt;String&gt; providers = <span class="keyword">new</span> ArrayList&lt;&gt;(mProviders.size());</span><br><span class="line">		<span class="keyword">for</span> (LocationProvider provider : mProviders) &#123;</span><br><span class="line">			String name = provider.getName();</span><br><span class="line">			<span class="keyword">if</span> (FUSED_PROVIDER.equals(name)) &#123;</span><br><span class="line">				<span class="keyword">continue</span>;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">if</span> (allowedResolutionLevel &lt; getMinimumResolutionLevelForProviderUseLocked(name)) &#123;</span><br><span class="line">                <span class="comment">// 权限与级别</span></span><br><span class="line">				<span class="keyword">continue</span>;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">if</span> (enabledOnly &amp;&amp; !provider.isUseableLocked()) &#123;</span><br><span class="line">                <span class="comment">// 判断获取的是需要可用，还是不可用的</span></span><br><span class="line">				<span class="keyword">continue</span>;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">if</span> (criteria != <span class="keyword">null</span></span><br><span class="line">					&amp;&amp; !android.location.LocationProvider.propertiesMeetCriteria(</span><br><span class="line">					name, provider.getPropertiesLocked(), criteria)) &#123;</span><br><span class="line">				<span class="keyword">continue</span>;</span><br><span class="line">			&#125;</span><br><span class="line">			providers.add(name);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> providers;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后使用方法：requestLocationUpdates，来最终获取地理位置信息</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">requestLocationUpdates</span><span class="params">(LocationRequest request, ILocationListener listener,</span></span></span><br><span class="line"><span class="function"><span class="params">        PendingIntent intent, String packageName)</span> </span>&#123;</span><br><span class="line">    ... <span class="comment">//代码判断权限</span></span><br><span class="line">    requestLocationUpdatesLocked(sanitizedRequest, receiver, uid, packageName);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">requestLocationUpdatesLocked</span><span class="params">(LocationRequest request, Receiver receiver,</span></span></span><br><span class="line"><span class="function"><span class="params">		<span class="keyword">int</span> uid, String packageName)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    applyRequirementsLocked(name);	</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">applyRequirementsLocked</span><span class="params">(LocationProvider provider)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// 核心方法，调用使用Provider的方法setRequestLocked，调用setRequest，请求地理位置</span></span><br><span class="line">    provider.setRequestLocked(providerRequest, worksource</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>调用请求setRequest后，Provider获取数据完成后，会调用LocationProviderBase的reportLocation方法，再调用ILocationProviderManager的onReportLocation回调数据。后续再通过ILocationListener回调至请求app，完成一次请求地理位置数据流程。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onReportLocation</span><span class="params">(Location location)</span> </span>&#123;    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Android</category>
      </categories>
  </entry>
  <entry>
    <title>ActivityManagerService简析</title>
    <url>/2020/07/14/ActivityManagerService%E7%AE%80%E6%9E%90/</url>
    <content><![CDATA[<h2 id="AMS启动"><a href="#AMS启动" class="headerlink" title="AMS启动"></a>AMS启动</h2><p>设备在开机启动时，启动SystemServe时会在startBootstrapServices方法会启动AMS服务</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">startBootstrapServices</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="comment">//...</span></span><br><span class="line">       mActivityManagerService = mSystemServiceManager.startService(</span><br><span class="line">               ActivityManagerService.Lifecycle<span class="class">.<span class="keyword">class</span>).<span class="title">getService</span>()</span>;</span><br><span class="line">       mActivityManagerService.setSystemServiceManager(mSystemServiceManager);</span><br><span class="line">       mActivityManagerService.setInstaller(installer);</span><br><span class="line">	<span class="comment">//...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在ActivityManagerService类中的Lifecycle会去新建对象</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">Lifecycle</span> <span class="keyword">extends</span> <span class="title">SystemService</span> </span>&#123;</span><br><span class="line"><span class="comment">//...</span></span><br><span class="line">      <span class="keyword">private</span> <span class="keyword">final</span> ActivityManagerService mService;</span><br><span class="line">      <span class="function"><span class="keyword">public</span> <span class="title">Lifecycle</span><span class="params">(Context context)</span> </span>&#123;</span><br><span class="line">          <span class="keyword">super</span>(context);</span><br><span class="line">          mService = <span class="keyword">new</span> ActivityManagerService(context);</span><br><span class="line">      &#125;</span><br><span class="line"><span class="comment">//...</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">ActivityManagerService</span><span class="params">(Context systemContext)</span> </span>&#123;</span><br><span class="line">  	<span class="comment">//...</span></span><br><span class="line">  	mStackSupervisor = createStackSupervisor();</span><br><span class="line">  	<span class="comment">//...</span></span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h2 id="AMS流程"><a href="#AMS流程" class="headerlink" title="AMS流程"></a>AMS流程</h2><p>在开始AMS流程前，先来熟悉一下Activity的周期，周期对应着后面AMS的解析流程<a id="more"></a></p>
<p><img src="/images/Activity%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F.jpg" alt=""></p>
<p>从界面开始执行startActivity时，涉及四个进程</p>
<p>A进程（启动） — &gt; SystemServer进程   — &gt; Zygote进程  — &gt; B进程（被启动进程）</p>
<p>下图是AMS调用的简易流程</p>
<p><img src="/images/AMS%E7%AE%80%E6%98%93%E6%B5%81%E7%A8%8B.jpg" alt=""></p>
<p>最后给出一个全流程图，AMS还是需要一步一步去断点跑每一个类与方法，这样更能理解AMS</p>
<p><img src="/images/AMS%E5%90%AF%E5%8A%A8%E6%B5%81%E7%A8%8B%E5%9B%BE.jpg" alt=""></p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
  </entry>
</search>
